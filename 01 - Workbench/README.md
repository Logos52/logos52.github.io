# 01 - Workbench

Canonical WIP/workbench folder for the vault. This is the active synthesis surface: model first-pass drafts (`[Model] - [Title].md`), fused L2 syntheses (`L2 - [Title].md`), comparisons, and status reports live here until they are promoted to `wiki/` or archived. Files inside are private working drafts (gitignored); this README is the one tracked file. `workbench.base` and the Home "what's in motion" embed filter on this folder.
